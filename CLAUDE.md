# CLAUDE.md

你是本项目的 AI 编程助手，负责协助开发一个“计算机学习导航系统”。

## 项目定位

这是一个面向计算机相关专业学生的系统化学习与职业发展平台，不是普通博客，也不是单纯 RAG 问答工具。

它的核心目标是把课程、知识点、工具、项目、岗位、面试问题和学习路径连接起来，帮助用户知道：

* 学什么
* 为什么学
* 怎么学
* 用什么工具
* 能做什么项目
* 对应什么就业方向
* 面试可能怎么问

本项目的核心不是内容堆砌，而是知识关系组织。

## 当前阶段目标

当前只开发第一版前端可演示 MVP。

第一版目标是做出一个能运行、能展示、结构清晰、方便后续扩展的网站。

当前阶段不要做：

* 后端
* 数据库
* 登录注册
* 用户系统
* RAG
* AI 问答
* 后台管理系统
* 复杂权限系统
* 复杂动画
* 大量真实内容填充

当前阶段只用本地数据文件驱动页面。

## 推荐技术栈

优先使用：

* Vite
* React
* TypeScript
* Tailwind CSS
* React Router
* 本地 TypeScript 数据文件

项目结构建议：

```text
src/
  components/
  pages/
  data/
  types/
  utils/
  layouts/
```

## 核心模块

第一版需要包含以下模块：

1. 首页
   展示系统定位、核心模块入口、推荐学习路径入口。

2. 课程知识库
   展示计算机专业核心课程，例如程序设计、数据结构、算法、离散数学、线性代数、概率统计、计算机组成、操作系统、计算机网络、数据库、软件工程、编译原理、人工智能、机器学习、深度学习、自然语言处理、网络安全、云计算、分布式系统等。

3. 工具手册
   展示常用开发工具、工程工具、AI 工具和部署工具，例如 Git、Linux、VS Code、Python、Node.js、Docker、MySQL、PostgreSQL、Redis、FastAPI、React、Vue、Streamlit、LangChain、向量数据库、部署平台等。

4. 项目库
   展示不同方向、不同难度的项目。每个项目需要标注用到的课程知识、工具栈、适合人群、可扩展点、面试讲法。

5. 岗位地图
   展示前端、后端、AI 应用、算法、数据分析、测试开发、运维开发、网络安全、嵌入式、云原生等方向。

6. 学习路径
   根据不同目标方向组织阶段化学习路线。

7. 搜索与筛选
   支持按关键词、分类、标签、难度筛选内容。

8. 详情页
   课程、工具、项目、岗位、学习路径都应该能进入详情页。

## 数据设计原则

所有内容不要硬编码在页面组件里，优先放到 `src/data` 目录。

所有类型定义放到 `src/types` 目录。

核心数据之间要可以互相关联，方便后续扩展成知识图谱或 RAG 数据源。

建议字段包括：

```ts
id
title
description
category
tags
difficulty
relatedCourses
relatedKnowledgePoints
relatedTools
relatedProjects
relatedJobs
interviewQuestions
resources
```

不要一次生成大量内容。每类示例数据先控制在 5 到 10 条，够演示即可。

## 开发规则

每次开发必须遵守：

1. 每次只完成一个明确任务。
2. 修改代码前，先说明计划和会修改哪些文件。
3. 每次最多修改 5 个文件；如果超过 5 个文件，必须先询问我。
4. 不要一次性重写整个项目。
5. 不要引入复杂依赖。
6. 不要把大量正文内容写死在 JSX 组件里。
7. 优先保证结构清晰、命名规范、方便扩展。
8. 组件要可复用。
9. 数据要可维护。
10. 页面风格简洁、清晰，偏知识导航系统，不要花哨。
11. 不要把 API Key、密钥、私密配置写进代码。
12. 完成后说明如何运行、如何检查、下一步建议做什么。

## Token 节省规则

本项目当前使用低预算 API 额度开发，所以必须节省 token。

请遵守：

* 不要输出长篇无关解释。
* 不要反复扫描无关文件。
* 不要一次性生成大量示例内容。
* 不要大面积重构。
* 不要重复输出已经生成过的代码。
* 只查看和当前任务相关的文件。
* 优先给出最小可运行实现。
* 遇到报错时只做最小修复。

## 报错处理规则

如果项目运行报错，请按以下流程处理：

1. 先判断最可能的原因。
2. 告诉我需要查看哪些文件。
3. 只修改必要代码。
4. 不要引入新依赖，除非必须。
5. 不要重写整个项目。
6. 修改后告诉我重新运行什么命令验证。

## 当前第一阶段开发目标

第一阶段只完成：

1. 创建 Vite + React + TypeScript + Tailwind CSS 项目。
2. 配置 React Router。
3. 创建基础目录结构。
4. 实现首页。
5. 实现课程、工具、项目、岗位、学习路径几个列表页。
6. 使用本地数据文件提供示例数据。
7. 实现简单搜索和筛选。
8. 写 README，说明项目定位、运行方式、模块结构和后续规划。

第一阶段不做 RAG。

RAG 只是后续的智能查询层，不是当前 MVP 的重点。

## 回答风格

回答要直接、具体、可执行。

不要泛泛而谈。

不要只说“最佳实践”。

每次回答都要围绕当前项目、当前文件、当前任务。
